import { OptionActivePipe } from './option-active.pipe';

describe('OptionActivePipe', () => {
  it('create an instance', () => {
    const pipe = new OptionActivePipe();
    expect(pipe).toBeTruthy();
  });
});
